overwrite: true
schema: "src/schema.graphql"
documents: null
generates:
  src/generated/graphql-types.ts:
    config:
      contextType: ../resolvers/Context#IContext
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
