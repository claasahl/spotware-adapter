type Query {
  hello(name: String): String!
}

type Mutation {
  tokens(code: String!): String!
  proto(code: String!): String!
}

type Subscription {
  somethingChanged: Result
}

type Result {
  id: String!
}
